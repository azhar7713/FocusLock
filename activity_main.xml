<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="24dp">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="اكتب الكلمات التي تريد رصدها، كلمة واحدة في كل سطر:"
        android:textSize="16sp"
        android:layout_marginBottom="12dp" />

    <EditText
        android:id="@+id/editTextWords"
        android:layout_width="match_parent"
        android:layout_height="200dp"
        android:gravity="top|start"
        android:inputType="textMultiLine"
        android:background="@android:drawable/edit_text" />

    <Button
        android:id="@+id/buttonSave"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:text="حفظ الكلمات" />

    <Button
        android:id="@+id/buttonEnableService"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="12dp"
        android:text="تفعيل خدمة إمكانية الوصول" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:text="عند رصد أي كلمة، سيتم إغلاق التطبيق المفتوح تلقائيًا (الرجوع للشاشة الرئيسية) وحظر فتحه مجددًا لمدة نصف ساعة."
        android:textSize="13sp"
        android:textColor="#666666" />

</LinearLayout>
